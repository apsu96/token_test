{
  "name": "token_test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^4.6.0",
    "dotenv": "^16.0.1",
    "hardhat": "^2.9.5",
    "hardhat-deploy": "^0.11.5",
    "hardhat-deploy-ethers": "^0.3.0-beta.13"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:rinkeby": "hardhat deploy --network rinkeby",
    "verify:rinkeby": "hardhat etherscan-verify --network rinkeby --api-key $ETHERSCAN_KEY"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.0.0",
    "ethers": "^5.6.6"
  }
}
